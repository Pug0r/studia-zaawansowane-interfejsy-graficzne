<Window x:Class="Lab09_speech.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab09_speech"
        mc:Ignorable="d"
        Title="Speech" Height="450" Width="800">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <Button x:Name="OpenFileButton" Content="Open" Width="80" Margin="0,0,8,0" Click="OpenFile_Click" />
            <Button x:Name="StartButton" Content="Start" Width="80" Click="Start_Click" />
            <Button x:Name="PauseButton" IsEnabled="False" Content="Pause" Width="80" Click="Pause_Click" />
        </StackPanel>

        <TextBox x:Name="TextContent"
                 Grid.Row="1"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" />
    </Grid>
</Window>
